# Approach:
# Use Python's built-in lower() method to convert every uppercase character
# in the string to lowercase.
#
# Time: O(n)
# Space: O(n)


class Solution:
    def toLowerCase(self, s: str) -> str:

        return s.lower()
